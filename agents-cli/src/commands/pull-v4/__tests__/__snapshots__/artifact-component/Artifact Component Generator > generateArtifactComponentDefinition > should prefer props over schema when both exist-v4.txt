import { artifactComponent } from '@agent-fabric/agents-sdk';
import { preview } from '@agent-fabric/agents-core';
import { z } from 'zod';

export const test = artifactComponent({
  id: 'test',
  name: 'Test',
  description: 'Component with both props and schema',
  props: z.object({
      prop: preview(z.string()),
    }),
});
