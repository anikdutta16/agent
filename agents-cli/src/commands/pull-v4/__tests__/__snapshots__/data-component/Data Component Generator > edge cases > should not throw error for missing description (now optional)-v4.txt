import { dataComponent } from '@agent-fabric/agents-sdk';
import { z } from 'zod';

export const missingDesc = dataComponent({
  id: 'missing-desc',
  name: 'Test Component',
  props: z.object({}),
});
