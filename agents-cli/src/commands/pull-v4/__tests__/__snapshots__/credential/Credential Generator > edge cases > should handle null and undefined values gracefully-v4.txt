import { credential } from '@agent-fabric/agents-sdk';

export const nullTestCredential = credential({
  id: 'null-test',
  name: 'Null Test Credential',
  type: 'memory',
  credentialStoreId: 'env-default',
  retrievalParams: {
    key: 'API_KEY',
  },
});
